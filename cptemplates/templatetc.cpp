#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
/*

*/

void solve(){
    int n;
    cin >> n;
    
}

int main(){
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin.tie(0);
    int T, I=1;
    cin >> T;
    while(--T) {
        cout << "Case #" << I << ": ";
        solve();
        ++I;
    }
    return 0;
}