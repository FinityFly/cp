#include <bits/stdc++.h>
using namespace std;

/*
INPUT:

 */

int main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int a, b, c, d, s, x=0, y=0;
    cin >> a >> b >> c >> d >> s;
    for (int i=0; i<s; ++i) {
        
    }
    return 0;
}