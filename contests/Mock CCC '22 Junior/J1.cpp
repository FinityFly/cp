#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
/*

*/

int main() {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    int a, b, c, x, y, z;
    cin >> a >> b >> c >> x >> y >> z;
    cout << x + y + z << " " << a * x + b * y + c * z << "\n";
    
    return 0;
}