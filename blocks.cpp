#include <bits/stdc++.h>
using namespace std;

/*
INPUT:

 */

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    ifstream fin("blocks.in");
    ofstream fout("blocks.out");
    int n, output[26];
    cin >> n;
    string words[n][2];
    int cur[n];
    for(int i=0;i<n;++i) cin >> words[i][0] >> words[i][1];


    return 0;
}