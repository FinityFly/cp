#include <bits/stdc++.h>
using namespace std;

/*
INPUT:

 */
struct node{
    int id;
    node* left;
    node* right;
};

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
//    ifstream fin("template.in");
//    ofstream fout("template.out");
    int n, a, b, x, y;
    vector< pair<string, vector<string> > > tree;
    cin >> n >> a >> b;
    for(int i=0;i<n;++i){
        cin >> x >> y;
    }
    return 0;
}